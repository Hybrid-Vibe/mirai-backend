diff --git a/Mirai/Mirai.Application/DTO/NanoBananaRequestDto.cs b/Mirai/Mirai.Application/DTO/NanoBananaRequestDto.cs
deleted file mode 100644
index 418e868..0000000
--- a/Mirai/Mirai.Application/DTO/NanoBananaRequestDto.cs
+++ /dev/null
@@ -1,24 +0,0 @@
-using System.Collections.Generic;
-
-namespace Mirai.Application.DTO;
-
-public class NanoBananaRequestDto
-{
-    public string Prompt { get; set; } = null!;
-
-    public string? NegativePrompt { get; set; }
-
-    public string? Style { get; set; }
-
-    public int? Width { get; set; }
-
-    public int? Height { get; set; }
-
-    public int? Steps { get; set; } = 20;
-
-    public float? CfgScale { get; set; } = 7.5f;
-
-    public string? Seed { get; set; }
-
-    public List<string>? Sampler { get; set; }
-}
diff --git a/Mirai/Mirai.Application/DTO/NanoBananaResponseDto.cs b/Mirai/Mirai.Application/DTO/NanoBananaResponseDto.cs
deleted file mode 100644
index cc8e81b..0000000
--- a/Mirai/Mirai.Application/DTO/NanoBananaResponseDto.cs
+++ /dev/null
@@ -1,20 +0,0 @@
-using System.Collections.Generic;
-
-namespace Mirai.Application.DTO;
-
-public class NanoBananaResponseDto
-{
-    public bool Success { get; set; }
-
-    public string? RequestId { get; set; }
-
-    public string? Status { get; set; }
-
-    public string? ImageUrl { get; set; }
-
-    public string? ThumbnailUrl { get; set; }
-
-    public string? ErrorMessage { get; set; }
-
-    public Dictionary<string, object>? Metadata { get; set; }
-}
diff --git a/Mirai/Mirai.Application/Interfaces/Services/INanoBananaService.cs b/Mirai/Mirai.Application/Interfaces/Services/INanoBananaService.cs
deleted file mode 100644
index 366e47c..0000000
--- a/Mirai/Mirai.Application/Interfaces/Services/INanoBananaService.cs
+++ /dev/null
@@ -1,11 +0,0 @@
-using Mirai.Application.DTO;
-using System.Threading;
-using System.Threading.Tasks;
-
-namespace Mirai.Application.Interfaces.Services;
-
-public interface INanoBananaService
-{
-    Task<NanoBananaResponseDto> GenerateImageAsync(NanoBananaRequestDto request, CancellationToken cancellationToken = default);
-    Task<NanoBananaResponseDto> GetImageStatusAsync(string requestId, CancellationToken cancellationToken = default);
-}
diff --git a/Mirai/Mirai.Domain/Entities/AIImage.cs b/Mirai/Mirai.Domain/Entities/AIImage.cs
index d5ebc12..9f8e7b1 100644
--- a/Mirai/Mirai.Domain/Entities/AIImage.cs
+++ b/Mirai/Mirai.Domain/Entities/AIImage.cs
@@ -27,7 +27,7 @@ public partial class AiImage
 
     public string? ErrorMessage { get; set; }
 
-    public string? NanoBananaRequestId { get; set; }
+    public string? ReplicatePredictionId { get; set; }
 
     public DateTime CreatedAt { get; set; }
 
diff --git a/Mirai/Mirai.Infastructure/Data/AppDbContext.cs b/Mirai/Mirai.Infastructure/Data/AppDbContext.cs
index 5211cde..2972653 100644
--- a/Mirai/Mirai.Infastructure/Data/AppDbContext.cs
+++ b/Mirai/Mirai.Infastructure/Data/AppDbContext.cs
@@ -1,4 +1,4 @@
-﻿using Microsoft.EntityFrameworkCore;
+using Microsoft.EntityFrameworkCore;
 using Microsoft.Extensions.Configuration;
 using Mirai.Domain.Entities;
 using System;
@@ -180,9 +180,9 @@ public partial class AppDbContext : DbContext
             entity.Property(e => e.ErrorMessage).HasColumnName("error_message");
             entity.Property(e => e.Height).HasColumnName("height");
             entity.Property(e => e.ImageUrl).HasColumnName("image_url");
-            entity.Property(e => e.NanoBananaRequestId)
+            entity.Property(e => e.ReplicatePredictionId)
                 .HasMaxLength(255)
-                .HasColumnName("nano_banana_request_id");
+                .HasColumnName("replicate_prediction_id");
             entity.Property(e => e.NegativePrompt).HasColumnName("negative_prompt");
             entity.Property(e => e.Prompt).HasColumnName("prompt");
             entity.Property(e => e.Status).HasColumnName("status");
diff --git a/Mirai/Mirai.Infastructure/DependencyInjection.cs b/Mirai/Mirai.Infastructure/DependencyInjection.cs
index ce04a64..14d573b 100644
--- a/Mirai/Mirai.Infastructure/DependencyInjection.cs
+++ b/Mirai/Mirai.Infastructure/DependencyInjection.cs
@@ -1,8 +1,10 @@
 ﻿using Microsoft.EntityFrameworkCore;
 using Microsoft.Extensions.Configuration;
 using Microsoft.Extensions.DependencyInjection;
+using Mirai.Application.Configuration;
 using Mirai.Application.Interfaces.Repositories;
 using Mirai.Application.Interfaces.Services;
+using Mirai.Infastructure.Clients;
 using Mirai.Infastructure.Data;
 using Mirai.Infastructure.Repositories;
 using Mirai.Infastructure.Services;
@@ -54,6 +56,7 @@ namespace Mirai.Infastructure
             services.AddScoped<IPaymentService, PaymentService>();
             services.AddScoped<ICartItemsService, CartItemsService>();
             services.AddScoped<IAIImageService, AIImageService>();
+            services.AddScoped<IImageGenerationService, ImageGenerationService>();
             services.AddScoped<IAdminService, AdminService>();
             services.AddScoped<IJwtTokenGenerator, JwtTokenGenerator>();
             services.AddScoped<ITurnstileService, TurnstileService>();
@@ -64,10 +67,12 @@ namespace Mirai.Infastructure
 
         private static IServiceCollection AddExternalClients(this IServiceCollection services, IConfiguration configuration)
         {
-            services.AddHttpClient<INanoBananaService, NanoBananaService>(client =>
+            services.Configure<ReplicateOptions>(configuration.GetSection(ReplicateOptions.SectionName));
+
+            services.AddHttpClient<IReplicateClient, ReplicateClient>(client =>
             {
-                var baseUrl = configuration["NanoBanana:BaseUrl"] ?? "https://api.nanobanana.ai";
-                client.BaseAddress = new Uri(baseUrl);
+                client.BaseAddress = new Uri("https://api.replicate.com/");
+                client.Timeout = TimeSpan.FromMinutes(3);
             });
 
             return services;
diff --git a/Mirai/Mirai.Infastructure/Repositories/AIImageRepository.cs b/Mirai/Mirai.Infastructure/Repositories/AIImageRepository.cs
index f5e1414..b46ac96 100644
--- a/Mirai/Mirai.Infastructure/Repositories/AIImageRepository.cs
+++ b/Mirai/Mirai.Infastructure/Repositories/AIImageRepository.cs
@@ -48,15 +48,15 @@ public class AIImageRepository : IAIImageRepository
         return Task.CompletedTask;
     }
 
-    public async Task<PagedResult<AIImage>> GetAllPagedAsync(AdminAIImageFilter filter, CancellationToken cancellationToken = default)
+    public async Task<PagedResult<AiImage>> GetAllPagedAsync(AdminAIImageFilter filter, CancellationToken cancellationToken = default)
     {
-        var query = _context.AIImages.AsNoTracking().AsQueryable();
+        var query = _context.AiImages.AsNoTracking().AsQueryable();
 
         if (!string.IsNullOrWhiteSpace(filter.UserId))
             query = query.Where(ai => ai.UserId == filter.UserId);
 
         if (filter.Status.HasValue)
-            query = query.Where(ai => ai.Status == filter.Status.Value);
+            query = query.Where(ai => ai.Status == (int)filter.Status.Value);
 
         var totalCount = await query.CountAsync(cancellationToken);
 
@@ -66,7 +66,7 @@ public class AIImageRepository : IAIImageRepository
             .Take(filter.PageSize)
             .ToListAsync(cancellationToken);
 
-        return new PagedResult<AIImage>
+        return new PagedResult<AiImage>
         {
             Items = items,
             TotalCount = totalCount,
@@ -89,13 +89,13 @@ public class AIImageRepository : IAIImageRepository
 
     public async Task<bool> AdminDeleteAsync(string id, CancellationToken cancellationToken = default)
     {
-        var aiImage = await _context.AIImages
-            .FirstOrDefaultAsync(ai => ai.AIImageId == id, cancellationToken);
+        var aiImage = await _context.AiImages
+            .FirstOrDefaultAsync(ai => ai.AiImageId == id, cancellationToken);
 
         if (aiImage == null)
             return false;
 
-        _context.AIImages.Remove(aiImage);
+        _context.AiImages.Remove(aiImage);
         return true;
     }
 
diff --git a/Mirai/Mirai.Infastructure/Repositories/AdminRepository.cs b/Mirai/Mirai.Infastructure/Repositories/AdminRepository.cs
index 066af00..a9a40ab 100644
--- a/Mirai/Mirai.Infastructure/Repositories/AdminRepository.cs
+++ b/Mirai/Mirai.Infastructure/Repositories/AdminRepository.cs
@@ -21,11 +21,11 @@ public class AdminRepository : IAdminRepository
 
     public async Task<AdminDashboardDto> GetDashboardSummaryAsync(CancellationToken cancellationToken = default)
     {
-        var paidStatus = PaymentStatus.Paid.ToString();
+        var paidStatus = (int)PaymentStatus.Paid;
         var pendingOrderStatuses = new[]
         {
-            OrderStatus.Created.ToString(),
-            OrderStatus.Confirmed.ToString()
+            (int)OrderStatus.Created,
+            (int)OrderStatus.Confirmed
         };
 
         return new AdminDashboardDto
@@ -34,7 +34,7 @@ public class AdminRepository : IAdminRepository
             ActiveUsers = await _context.Users.CountAsync(u => u.IsActive, cancellationToken),
             TotalOrders = await _context.Orders.CountAsync(cancellationToken),
             PendingOrders = await _context.Orders.CountAsync(
-                o => o.Status != null && pendingOrderStatuses.Contains(o.Status),
+                o => o.Status != null && pendingOrderStatuses.Contains(o.Status.Value),
                 cancellationToken),
             TotalRevenue = await _context.Orders
                 .Where(o => o.PaymentStatus == paidStatus)
@@ -48,7 +48,7 @@ public class AdminRepository : IAdminRepository
 
     public async Task<AdminRevenueChartDto> GetRevenueChartAsync(string period, CancellationToken cancellationToken = default)
     {
-        var paidStatus = PaymentStatus.Paid.ToString();
+        var paidStatus = (int)PaymentStatus.Paid;
         var isMonth = string.Equals(period, "month", StringComparison.OrdinalIgnoreCase);
         var normalizedPeriod = isMonth ? "month" : "week";
         var today = DateTime.UtcNow.Date;
@@ -224,11 +224,13 @@ public class AdminRepository : IAdminRepository
             .Include(o => o.OrderItems)
             .AsQueryable();
 
-        if (!string.IsNullOrWhiteSpace(filter.Status))
-            query = query.Where(o => o.Status == filter.Status);
+        if (!string.IsNullOrWhiteSpace(filter.Status)
+            && Enum.TryParse<OrderStatus>(filter.Status, true, out var orderStatus))
+            query = query.Where(o => o.Status == (int)orderStatus);
 
-        if (!string.IsNullOrWhiteSpace(filter.PaymentStatus))
-            query = query.Where(o => o.PaymentStatus == filter.PaymentStatus);
+        if (!string.IsNullOrWhiteSpace(filter.PaymentStatus)
+            && Enum.TryParse<PaymentStatus>(filter.PaymentStatus, true, out var paymentStatus))
+            query = query.Where(o => o.PaymentStatus == (int)paymentStatus);
 
         if (!string.IsNullOrWhiteSpace(filter.UserId))
             query = query.Where(o => o.UserId == filter.UserId);
@@ -249,25 +251,39 @@ public class AdminRepository : IAdminRepository
 
         var totalCount = await query.CountAsync(cancellationToken);
 
-        var items = await query
+        var rows = await query
             .OrderByDescending(o => o.CreatedAt)
             .Skip((filter.PageNumber - 1) * filter.PageSize)
             .Take(filter.PageSize)
-            .Select(o => new AdminOrderListDto
+            .Select(o => new
             {
-                OrderId = o.OrderId,
-                OrderNumber = o.OrderNumber,
-                UserId = o.UserId,
+                o.OrderId,
+                o.OrderNumber,
+                o.UserId,
                 UserEmail = o.User.Email,
                 UserFullName = o.User.FullName,
-                TotalAmount = o.TotalAmount,
-                Status = o.Status,
-                PaymentStatus = o.PaymentStatus,
-                CreatedAt = o.CreatedAt,
+                o.TotalAmount,
+                o.Status,
+                o.PaymentStatus,
+                o.CreatedAt,
                 ItemCount = o.OrderItems.Count
             })
             .ToListAsync(cancellationToken);
 
+        var items = rows.Select(o => new AdminOrderListDto
+        {
+            OrderId = o.OrderId,
+            OrderNumber = o.OrderNumber,
+            UserId = o.UserId,
+            UserEmail = o.UserEmail,
+            UserFullName = o.UserFullName,
+            TotalAmount = o.TotalAmount,
+            Status = ToOrderStatusString(o.Status),
+            PaymentStatus = ToPaymentStatusString(o.PaymentStatus),
+            CreatedAt = o.CreatedAt,
+            ItemCount = o.ItemCount
+        }).ToList();
+
         return new PagedResult<AdminOrderListDto>
         {
             Items = items,
@@ -301,8 +317,8 @@ public class AdminRepository : IAdminRepository
             TaxAmount = order.TaxAmount,
             TotalAmount = order.TotalAmount,
             Currency = order.Currency,
-            Status = order.Status,
-            PaymentStatus = order.PaymentStatus,
+            Status = ToOrderStatusString(order.Status),
+            PaymentStatus = ToPaymentStatusString(order.PaymentStatus),
             Note = order.Note,
             CreatedAt = order.CreatedAt,
             PlacedAt = order.PlacedAt,
@@ -321,9 +337,9 @@ public class AdminRepository : IAdminRepository
                 PaymentId = p.PaymentId,
                 OrderId = p.OrderId,
                 OrderNumber = order.OrderNumber,
-                Method = p.Method,
-                Provider = p.Provider,
-                Status = p.Status,
+                Method = ToPaymentMethodString(p.Method),
+                Provider = ToPaymentMethodString(p.Provider),
+                Status = ToPaymentInPaymentStatusString(p.Status),
                 Amount = p.Amount,
                 TransactionId = p.TransactionId,
                 PaidAt = p.PaidAt,
@@ -335,7 +351,7 @@ public class AdminRepository : IAdminRepository
                 OrderId = s.OrderId,
                 OrderNumber = order.OrderNumber,
                 AddressId = s.AddressId,
-                ShippingStatus = s.ShippingStatus,
+                ShippingStatus = ToShippingStatusString(s.ShippingStatus),
                 Carrier = s.Carrier,
                 TrackingCode = s.TrackingCode,
                 ShippingFee = s.ShippingFee,
@@ -352,33 +368,48 @@ public class AdminRepository : IAdminRepository
             .Include(p => p.Order)
             .AsQueryable();
 
-        if (!string.IsNullOrWhiteSpace(filter.Status))
-            query = query.Where(p => p.Status == filter.Status);
+        if (!string.IsNullOrWhiteSpace(filter.Status)
+            && Enum.TryParse<PaymentStatusInPayment>(filter.Status, true, out var paymentStatus))
+            query = query.Where(p => p.Status == (int)paymentStatus);
 
         if (!string.IsNullOrWhiteSpace(filter.OrderId))
             query = query.Where(p => p.OrderId == filter.OrderId);
 
         var totalCount = await query.CountAsync(cancellationToken);
 
-        var items = await query
+        var rows = await query
             .OrderByDescending(p => p.CreatedAt)
             .Skip((filter.PageNumber - 1) * filter.PageSize)
             .Take(filter.PageSize)
-            .Select(p => new AdminPaymentDto
+            .Select(p => new
             {
-                PaymentId = p.PaymentId,
-                OrderId = p.OrderId,
+                p.PaymentId,
+                p.OrderId,
                 OrderNumber = p.Order.OrderNumber,
-                Method = p.Method,
-                Provider = p.Provider,
-                Status = p.Status,
-                Amount = p.Amount,
-                TransactionId = p.TransactionId,
-                PaidAt = p.PaidAt,
-                CreatedAt = p.CreatedAt
+                p.Method,
+                p.Provider,
+                p.Status,
+                p.Amount,
+                p.TransactionId,
+                p.PaidAt,
+                p.CreatedAt
             })
             .ToListAsync(cancellationToken);
 
+        var items = rows.Select(p => new AdminPaymentDto
+        {
+            PaymentId = p.PaymentId,
+            OrderId = p.OrderId,
+            OrderNumber = p.OrderNumber,
+            Method = ToPaymentMethodString(p.Method),
+            Provider = ToPaymentMethodString(p.Provider),
+            Status = ToPaymentInPaymentStatusString(p.Status),
+            Amount = p.Amount,
+            TransactionId = p.TransactionId,
+            PaidAt = p.PaidAt,
+            CreatedAt = p.CreatedAt
+        }).ToList();
+
         return new PagedResult<AdminPaymentDto>
         {
             Items = items,
@@ -462,31 +493,47 @@ public class AdminRepository : IAdminRepository
         if (!string.IsNullOrWhiteSpace(filter.OrderId))
             query = query.Where(s => s.OrderId == filter.OrderId);
 
-        if (!string.IsNullOrWhiteSpace(filter.ShippingStatus))
-            query = query.Where(s => s.ShippingStatus == filter.ShippingStatus);
+        if (!string.IsNullOrWhiteSpace(filter.ShippingStatus)
+            && int.TryParse(filter.ShippingStatus, out var shippingStatus))
+            query = query.Where(s => s.ShippingStatus == shippingStatus);
 
         var totalCount = await query.CountAsync(cancellationToken);
 
-        var items = await query
+        var rows = await query
             .OrderByDescending(s => s.CreatedAt)
             .Skip((filter.PageNumber - 1) * filter.PageSize)
             .Take(filter.PageSize)
-            .Select(s => new AdminShippingDto
+            .Select(s => new
             {
-                ShippingId = s.ShippingId,
-                OrderId = s.OrderId,
+                s.ShippingId,
+                s.OrderId,
                 OrderNumber = s.Order.OrderNumber,
-                AddressId = s.AddressId,
-                ShippingStatus = s.ShippingStatus,
-                Carrier = s.Carrier,
-                TrackingCode = s.TrackingCode,
-                ShippingFee = s.ShippingFee,
-                ShippedAt = s.ShippedAt,
-                DeliveredAt = s.DeliveredAt,
-                CreatedAt = s.CreatedAt
+                s.AddressId,
+                s.ShippingStatus,
+                s.Carrier,
+                s.TrackingCode,
+                s.ShippingFee,
+                s.ShippedAt,
+                s.DeliveredAt,
+                s.CreatedAt
             })
             .ToListAsync(cancellationToken);
 
+        var items = rows.Select(s => new AdminShippingDto
+        {
+            ShippingId = s.ShippingId,
+            OrderId = s.OrderId,
+            OrderNumber = s.OrderNumber,
+            AddressId = s.AddressId,
+            ShippingStatus = ToShippingStatusString(s.ShippingStatus),
+            Carrier = s.Carrier,
+            TrackingCode = s.TrackingCode,
+            ShippingFee = s.ShippingFee,
+            ShippedAt = s.ShippedAt,
+            DeliveredAt = s.DeliveredAt,
+            CreatedAt = s.CreatedAt
+        }).ToList();
+
         return new PagedResult<AdminShippingDto>
         {
             Items = items,
@@ -498,24 +545,41 @@ public class AdminRepository : IAdminRepository
 
     public async Task<AdminShippingDto?> GetShippingByIdAsync(string shippingId, CancellationToken cancellationToken = default)
     {
-        return await _context.Shippings
+        var row = await _context.Shippings
             .Include(s => s.Order)
             .Where(s => s.ShippingId == shippingId)
-            .Select(s => new AdminShippingDto
+            .Select(s => new
             {
-                ShippingId = s.ShippingId,
-                OrderId = s.OrderId,
+                s.ShippingId,
+                s.OrderId,
                 OrderNumber = s.Order.OrderNumber,
-                AddressId = s.AddressId,
-                ShippingStatus = s.ShippingStatus,
-                Carrier = s.Carrier,
-                TrackingCode = s.TrackingCode,
-                ShippingFee = s.ShippingFee,
-                ShippedAt = s.ShippedAt,
-                DeliveredAt = s.DeliveredAt,
-                CreatedAt = s.CreatedAt
+                s.AddressId,
+                s.ShippingStatus,
+                s.Carrier,
+                s.TrackingCode,
+                s.ShippingFee,
+                s.ShippedAt,
+                s.DeliveredAt,
+                s.CreatedAt
             })
             .FirstOrDefaultAsync(cancellationToken);
+
+        if (row == null) return null;
+
+        return new AdminShippingDto
+        {
+            ShippingId = row.ShippingId,
+            OrderId = row.OrderId,
+            OrderNumber = row.OrderNumber,
+            AddressId = row.AddressId,
+            ShippingStatus = ToShippingStatusString(row.ShippingStatus),
+            Carrier = row.Carrier,
+            TrackingCode = row.TrackingCode,
+            ShippingFee = row.ShippingFee,
+            ShippedAt = row.ShippedAt,
+            DeliveredAt = row.DeliveredAt,
+            CreatedAt = row.CreatedAt
+        };
     }
 
     public async Task<AdminShippingDto> CreateShippingAsync(AdminCreateShippingDto dto, CancellationToken cancellationToken = default)
@@ -533,7 +597,7 @@ public class AdminRepository : IAdminRepository
             ShippingId = Guid.NewGuid().ToString(),
             OrderId = dto.OrderId,
             AddressId = dto.AddressId,
-            ShippingStatus = dto.ShippingStatus ?? "Pending",
+            ShippingStatus = ParseShippingStatus(dto.ShippingStatus),
             Carrier = dto.Carrier,
             TrackingCode = dto.TrackingCode,
             ShippingFee = dto.ShippingFee,
@@ -551,7 +615,8 @@ public class AdminRepository : IAdminRepository
         var shipping = await _context.Shippings.FirstOrDefaultAsync(s => s.ShippingId == shippingId, cancellationToken);
         if (shipping == null) return false;
 
-        if (dto.ShippingStatus != null) shipping.ShippingStatus = dto.ShippingStatus;
+        if (dto.ShippingStatus != null)
+            shipping.ShippingStatus = ParseShippingStatus(dto.ShippingStatus);
         if (dto.Carrier != null) shipping.Carrier = dto.Carrier;
         if (dto.TrackingCode != null) shipping.TrackingCode = dto.TrackingCode;
         if (dto.ShippingFee.HasValue) shipping.ShippingFee = dto.ShippingFee;
@@ -576,4 +641,22 @@ public class AdminRepository : IAdminRepository
 
     public Task<bool> DeleteProductAsync(string productId, CancellationToken cancellationToken = default)
         => SetProductActiveAsync(productId, false, cancellationToken);
+
+    private static string? ToOrderStatusString(int? status)
+        => status.HasValue ? ((OrderStatus)status.Value).ToString() : null;
+
+    private static string? ToPaymentStatusString(int? status)
+        => status.HasValue ? ((PaymentStatus)status.Value).ToString() : null;
+
+    private static string? ToPaymentInPaymentStatusString(int? status)
+        => status.HasValue ? ((PaymentStatusInPayment)status.Value).ToString() : null;
+
+    private static string? ToPaymentMethodString(int? method)
+        => method.HasValue ? ((PaymentMethod)method.Value).ToString() : null;
+
+    private static string? ToShippingStatusString(int? status)
+        => status?.ToString();
+
+    private static int ParseShippingStatus(string? status)
+        => int.TryParse(status, out var value) ? value : 0;
 }
diff --git a/Mirai/Mirai.Infastructure/Repositories/PaymentRepository.cs b/Mirai/Mirai.Infastructure/Repositories/PaymentRepository.cs
index 23ca7f6..4665d46 100644
--- a/Mirai/Mirai.Infastructure/Repositories/PaymentRepository.cs
+++ b/Mirai/Mirai.Infastructure/Repositories/PaymentRepository.cs
@@ -61,14 +61,14 @@ namespace Mirai.Infastructure.Repositories
                 .FirstOrDefaultAsync(p => p.PaymentId == paymentId)
                 ?? throw new Exception("Payment not found");
 
-            if (!Enum.TryParse(payment.Status, out PaymentStatusInPayment currentStatus))
-            {
+            if (!payment.Status.HasValue || !Enum.IsDefined(typeof(PaymentStatusInPayment), payment.Status.Value))
                 throw new Exception($"Invalid payment status in DB: {payment.Status}");
-            }
+
+            var currentStatus = (PaymentStatusInPayment)payment.Status.Value;
             if (!PaymentStateValidator.CanUpdatePaymentStatus(currentStatus, newStatus))
                 throw new Exception($"Invalid payment status transition: {payment.Status} -> {newStatus}");
 
-            payment.Status = newStatus.ToString();
+            payment.Status = (int)newStatus;
             payment.UpdatedAt = DateTime.Now;
 
             await _context.SaveChangesAsync();
diff --git a/Mirai/Mirai.Infastructure/Services/AIImageService.cs b/Mirai/Mirai.Infastructure/Services/AIImageService.cs
index 1950215..69f1a86 100644
--- a/Mirai/Mirai.Infastructure/Services/AIImageService.cs
+++ b/Mirai/Mirai.Infastructure/Services/AIImageService.cs
@@ -5,22 +5,22 @@ using Mirai.Application.Interfaces.Repositories;
 using Mirai.Application.Interfaces.Services;
 using Mirai.Domain.Entities;
 using Mirai.Domain.Enums;
-using System;
-using System.Threading;
-using System.Threading.Tasks;
 
 namespace Mirai.Infastructure.Services;
 
 public class AIImageService : IAIImageService
 {
     private readonly IUnitOfWork _unitOfWork;
-    private readonly INanoBananaService _nanoBananaService;
+    private readonly IImageGenerationService _imageGenerationService;
     private readonly ILogger<AIImageService> _logger;
 
-    public AIImageService(IUnitOfWork unitOfWork, INanoBananaService nanoBananaService, ILogger<AIImageService> logger)
+    public AIImageService(
+        IUnitOfWork unitOfWork,
+        IImageGenerationService imageGenerationService,
+        ILogger<AIImageService> logger)
     {
         _unitOfWork = unitOfWork;
-        _nanoBananaService = nanoBananaService;
+        _imageGenerationService = imageGenerationService;
         _logger = logger;
     }
 
@@ -28,6 +28,9 @@ public class AIImageService : IAIImageService
     {
         _logger.LogInformation("Creating AI image for user {UserId}", userId);
 
+        var width = createDto.Width ?? 1024;
+        var height = createDto.Height ?? 576;
+
         var aiImage = new AiImage
         {
             AiImageId = Guid.NewGuid().ToString(),
@@ -35,51 +38,61 @@ public class AIImageService : IAIImageService
             Prompt = createDto.Prompt,
             NegativePrompt = createDto.NegativePrompt,
             Style = createDto.Style,
-            Width = createDto.Width ?? 512,
-            Height = createDto.Height ?? 512,
+            Width = width,
+            Height = height,
             Status = (int)AIImageStatus.Pending,
-            CreatedAt = DateTime.Now
+            CreatedAt = DateTime.UtcNow
         };
 
-        var nanoBananaRequest = new NanoBananaRequestDto
+        var generationRequest = new ImageGenerationRequest
         {
             Prompt = createDto.Prompt,
             NegativePrompt = createDto.NegativePrompt,
             Style = createDto.Style,
-            Width = createDto.Width ?? 512,
-            Height = createDto.Height ?? 512
+            Width = width,
+            Height = height
         };
 
         try
         {
-            _logger.LogInformation("Calling NanoBanana API for user {UserId}", userId);
-            var nanoBananaResponse = await _nanoBananaService.GenerateImageAsync(nanoBananaRequest, cancellationToken);
-            
-            if (nanoBananaResponse.Success)
+            aiImage.Status = (int)AIImageStatus.Processing;
+
+            _logger.LogInformation("Calling Replicate FLUX Schnell for user {UserId}", userId);
+            var generationResult = await _imageGenerationService.GenerateAsync(generationRequest, cancellationToken);
+
+            aiImage.ReplicatePredictionId = generationResult.PredictionId;
+
+            if (generationResult.Success)
             {
-                aiImage.NanoBananaRequestId = nanoBananaResponse.RequestId;
-                aiImage.Status = (int)AIImageStatus.Processing;
-                
-                if (!string.IsNullOrEmpty(nanoBananaResponse.ImageUrl))
-                {
-                    aiImage.ImageUrl = nanoBananaResponse.ImageUrl;
-                    aiImage.ThumbnailUrl = nanoBananaResponse.ThumbnailUrl;
-                    aiImage.Status = (int)AIImageStatus.Completed;
-                    _logger.LogInformation("AI image generated successfully for user {UserId}, ImageId: {ImageId}", userId, aiImage.AiImageId);
-                }
+                aiImage.ImageUrl = generationResult.ImageUrl;
+                aiImage.ThumbnailUrl = generationResult.ThumbnailUrl ?? generationResult.ImageUrl;
+                aiImage.Status = (int)AIImageStatus.Completed;
+                aiImage.UpdatedAt = DateTime.UtcNow;
+
+                _logger.LogInformation(
+                    "AI image generated via Replicate for user {UserId}, ImageId: {ImageId}, PredictionId: {PredictionId}",
+                    userId,
+                    aiImage.AiImageId,
+                    generationResult.PredictionId);
             }
             else
             {
                 aiImage.Status = (int)AIImageStatus.Failed;
-                aiImage.ErrorMessage = nanoBananaResponse.ErrorMessage;
-                _logger.LogWarning("NanoBanana API failed for user {UserId}: {Error}", userId, nanoBananaResponse.ErrorMessage);
+                aiImage.ErrorMessage = generationResult.ErrorMessage;
+                aiImage.UpdatedAt = DateTime.UtcNow;
+
+                _logger.LogWarning(
+                    "Replicate generation failed for user {UserId}: {Error}",
+                    userId,
+                    generationResult.ErrorMessage);
             }
         }
         catch (Exception ex)
         {
-            _logger.LogError(ex, "Error generating AI image for user {UserId}", userId);
+            _logger.LogError(ex, "Failed to create AI image for user {UserId}", userId);
             aiImage.Status = (int)AIImageStatus.Failed;
             aiImage.ErrorMessage = ex.Message;
+            aiImage.UpdatedAt = DateTime.UtcNow;
         }
 
         await _unitOfWork.AIImageRepository.AddAsync(aiImage, cancellationToken);
@@ -91,7 +104,7 @@ public class AIImageService : IAIImageService
     public async Task<AIImageDto?> GetAIImageByIdAsync(string aiImageId, CancellationToken cancellationToken = default)
     {
         _logger.LogInformation("Getting AI image by ID {ImageId}", aiImageId);
-        
+
         var aiImage = await _unitOfWork.AIImageRepository.GetByIdAsync(aiImageId, cancellationToken);
         return aiImage == null ? null : MapToDto(aiImage);
     }
@@ -99,7 +112,7 @@ public class AIImageService : IAIImageService
     public async Task<List<AIImageDto>> GetAIImagesByUserIdAsync(string userId, CancellationToken cancellationToken = default)
     {
         _logger.LogInformation("Getting AI images for user {UserId}", userId);
-        
+
         var aiImages = await _unitOfWork.AIImageRepository.GetByUserIdAsync(userId, cancellationToken);
         return aiImages.Select(MapToDto).ToList();
     }
@@ -146,7 +159,7 @@ public class AIImageService : IAIImageService
         }
 
         var result = await _unitOfWork.AIImageRepository.DeleteAsync(aiImageId, userId, cancellationToken);
-        
+
         if (result)
         {
             await _unitOfWork.SaveChangesAsync(cancellationToken);
@@ -174,7 +187,7 @@ public class AIImageService : IAIImageService
             Style = aiImage.Style,
             Width = aiImage.Width,
             Height = aiImage.Height,
-            Status = (AIImageStatus)aiImage.Status,
+            Status = (AIImageStatus)(aiImage.Status ?? (int)AIImageStatus.Pending),
             ErrorMessage = aiImage.ErrorMessage,
             CreatedAt = aiImage.CreatedAt,
             UpdatedAt = aiImage.UpdatedAt
diff --git a/Mirai/Mirai.Infastructure/Services/AdminService.cs b/Mirai/Mirai.Infastructure/Services/AdminService.cs
index 5a8f705..b9fff90 100644
--- a/Mirai/Mirai.Infastructure/Services/AdminService.cs
+++ b/Mirai/Mirai.Infastructure/Services/AdminService.cs
@@ -8,7 +8,7 @@ using Mirai.Application.Interfaces.Services;
 using Mirai.Application.SearchFilter.Admin;
 using Mirai.Domain.Entities;
 using Mirai.Domain.Enum;
-using Mirai.Domain.Enums;
+using AIImageStatus = Mirai.Domain.Enums.AIImageStatus;
 
 namespace Mirai.Infastructure.Services;
 
@@ -189,16 +189,16 @@ public class AdminService : IAdminService
     {
         var exists = await _unitOfWork.AIImageRepository.ExistsAsync(aiImageId, cancellationToken);
         if (!exists)
-            throw new NotFoundException(nameof(AIImage), aiImageId);
+            throw new NotFoundException(nameof(AiImage), aiImageId);
 
         var deleted = await _unitOfWork.AIImageRepository.AdminDeleteAsync(aiImageId, cancellationToken);
         if (deleted)
             await _unitOfWork.SaveChangesAsync(cancellationToken);
     }
 
-    private static AIImageDto MapAIImage(AIImage aiImage) => new()
+    private static AIImageDto MapAIImage(AiImage aiImage) => new()
     {
-        AIImageId = aiImage.AIImageId,
+        AIImageId = aiImage.AiImageId,
         UserId = aiImage.UserId,
         Prompt = aiImage.Prompt,
         NegativePrompt = aiImage.NegativePrompt,
@@ -207,7 +207,7 @@ public class AdminService : IAdminService
         Style = aiImage.Style,
         Width = aiImage.Width,
         Height = aiImage.Height,
-        Status = aiImage.Status,
+        Status = (AIImageStatus)(aiImage.Status ?? (int)AIImageStatus.Pending),
         ErrorMessage = aiImage.ErrorMessage,
         CreatedAt = aiImage.CreatedAt,
         UpdatedAt = aiImage.UpdatedAt
diff --git a/Mirai/Mirai.Infastructure/Services/NanoBananaService.cs b/Mirai/Mirai.Infastructure/Services/NanoBananaService.cs
deleted file mode 100644
index 94163ac..0000000
--- a/Mirai/Mirai.Infastructure/Services/NanoBananaService.cs
+++ /dev/null
@@ -1,124 +0,0 @@
-using Microsoft.Extensions.Configuration;
-using Microsoft.Extensions.Logging;
-using Mirai.Application.DTO;
-using Mirai.Application.Interfaces.Services;
-using System;
-using System.Net.Http;
-using System.Text;
-using System.Text.Json;
-using System.Threading;
-using System.Threading.Tasks;
-
-namespace Mirai.Infastructure.Services;
-
-public class NanoBananaService : INanoBananaService
-{
-    private readonly HttpClient _httpClient;
-    private readonly IConfiguration _configuration;
-    private readonly ILogger<NanoBananaService> _logger;
-
-    public NanoBananaService(HttpClient httpClient, IConfiguration configuration, ILogger<NanoBananaService> logger)
-    {
-        _httpClient = httpClient;
-        _configuration = configuration;
-        _logger = logger;
-    }
-
-    public async Task<NanoBananaResponseDto> GenerateImageAsync(NanoBananaRequestDto request, CancellationToken cancellationToken = default)
-    {
-        try
-        {
-            var jsonContent = JsonSerializer.Serialize(request);
-            var content = new StringContent(jsonContent, Encoding.UTF8, "application/json");
-
-            var apiKey = _configuration["NanoBanana:ApiKey"];
-            const string modelPath = "/v1beta/models/gemini-1.5-flash-latest:generateContent";
-            var requestUrl = $"{modelPath}?key={apiKey}";
-
-            _logger.LogInformation("Calling Google Gemini API at: {Path}", modelPath);
-            var response = await _httpClient.PostAsync(requestUrl, content, cancellationToken);
-
-            if (response.IsSuccessStatusCode)
-            {
-                var responseContent = await response.Content.ReadAsStringAsync(cancellationToken);
-                var result = JsonSerializer.Deserialize<NanoBananaResponseDto>(responseContent, new JsonSerializerOptions
-                {
-                    PropertyNameCaseInsensitive = true
-                });
-                
-                _logger.LogInformation("NanoBanana API call successful");
-                return result ?? new NanoBananaResponseDto { Success = false, ErrorMessage = "Failed to parse response" };
-            }
-            else
-            {
-                var errorContent = await response.Content.ReadAsStringAsync(cancellationToken);
-                _logger.LogError("NanoBanana API error: {StatusCode} - {Content}", response.StatusCode, errorContent);
-                
-                return new NanoBananaResponseDto 
-                { 
-                    Success = false, 
-                    ErrorMessage = $"API Error: {response.StatusCode} - {errorContent}" 
-                };
-            }
-        }
-        catch (OperationCanceledException)
-        {
-            _logger.LogWarning("NanoBanana API call was cancelled");
-            throw;
-        }
-        catch (Exception ex)
-        {
-            _logger.LogError(ex, "Error calling NanoBanana API");
-            return new NanoBananaResponseDto 
-            { 
-                Success = false, 
-                ErrorMessage = $"Exception: {ex.Message}" 
-            };
-        }
-    }
-
-    public async Task<NanoBananaResponseDto> GetImageStatusAsync(string requestId, CancellationToken cancellationToken = default)
-    {
-        try
-        {
-            _logger.LogInformation("Checking NanoBanana image status for RequestId: {RequestId}", requestId);
-            var response = await _httpClient.GetAsync($"/v1/image/status/{requestId}", cancellationToken);
-            
-            if (response.IsSuccessStatusCode)
-            {
-                var responseContent = await response.Content.ReadAsStringAsync(cancellationToken);
-                var result = JsonSerializer.Deserialize<NanoBananaResponseDto>(responseContent, new JsonSerializerOptions
-                {
-                    PropertyNameCaseInsensitive = true
-                });
-                
-                return result ?? new NanoBananaResponseDto { Success = false, ErrorMessage = "Failed to parse response" };
-            }
-            else
-            {
-                var errorContent = await response.Content.ReadAsStringAsync(cancellationToken);
-                _logger.LogError("NanoBanana status check error: {StatusCode} - {Content}", response.StatusCode, errorContent);
-                
-                return new NanoBananaResponseDto 
-                { 
-                    Success = false, 
-                    ErrorMessage = $"Status Check Error: {response.StatusCode}" 
-                };
-            }
-        }
-        catch (OperationCanceledException)
-        {
-            _logger.LogWarning("NanoBanana status check was cancelled");
-            throw;
-        }
-        catch (Exception ex)
-        {
-            _logger.LogError(ex, "Error checking NanoBanana image status");
-            return new NanoBananaResponseDto 
-            { 
-                Success = false, 
-                ErrorMessage = $"Exception: {ex.Message}" 
-            };
-        }
-    }
-}
diff --git a/Mirai/Mirai/appsettings.Development.json b/Mirai/Mirai/appsettings.Development.json
index 1b2d3ba..1e00892 100644
--- a/Mirai/Mirai/appsettings.Development.json
+++ b/Mirai/Mirai/appsettings.Development.json
@@ -4,5 +4,9 @@
       "Default": "Information",
       "Microsoft.AspNetCore": "Warning"
     }
+  },
+  "Replicate": {
+    "ApiToken": "",
+    "Model": "black-forest-labs/flux-schnell"
   }
-}
\ No newline at end of file
+}
